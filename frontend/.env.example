# React Frontend Environment Variables
# Copy this to .env.local for local development

# API Base URL (leave empty for same-origin during development)
VITE_API_BASE_URL=

# Default Tenant ID
VITE_DEFAULT_TENANT_ID=default
